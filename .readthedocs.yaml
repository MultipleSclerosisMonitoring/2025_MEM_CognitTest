version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

  jobs:
    pre_build:
      - dart pub get
      - dart doc
      - mkdir -p docs/source/_static/dartdoc
      - cp -r doc/api/* docs/source/_static/dartdoc/